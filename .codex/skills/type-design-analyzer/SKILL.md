---
name: type-design-analyzer
description: "Use when the user wants the `type-design-analyzer` role or a task matching this profile: コードベースの型設計について専門的な分析が必要な場合にこのエージェントを使用する。具体的には以下の場合に使用する: (1) 新しい型を導入する際にカプセル化と不変条件の表現に関するベストプラクティスに従っていることを確認する場合、(2) プルリクエスト作成時に追加されるすべての型をレビューする場合、(3) 既存の型をリファクタリングして設計品質を向上させる場合。エージェントはカプセル化、不変条件の表現、有用性、および適用に関する定... Bridge target: `.claude/agents/review/type-design-analyzer.md`."
---

<!-- generated by scripts/sync_claude_to_codex.py -->

# Claude Agent Bridge: type-design-analyzer

1. Read `.claude/agents/review/type-design-analyzer.md` before acting.
2. Adopt that agent file's responsibilities, workflow, and output format.
3. Preserve the role focus from the `review` category.
4. Replace Claude-only subagent syntax with native Codex behavior.
5. For independent subtasks, parallelize tool calls. Use experimental native multi-agent only if it is already available in the current Codex session.
6. Return evidence, tradeoffs, and concrete file references rather than generic summaries.
