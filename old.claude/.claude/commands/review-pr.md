---
description: "特化エージェントを使用した包括的な PR レビュー"
argument-hint: "[レビュー観点]"
allowed-tools: ["Bash", "Glob", "Grep", "Read", "Task"]
---

# 包括的な PR レビュー

複数の特化エージェントを使用して、それぞれがコード品質の異なる側面に焦点を当てた包括的なプルリクエストレビューを実行します。

**レビュー観点（任意）:** "$ARGUMENTS"

## レビューワークフロー:

1. **レビュースコープの決定**
   - git status で変更されたファイルを特定
   - 引数を解析してユーザーが特定のレビュー観点を要求しているか確認
   - デフォルト: 該当するすべてのレビューを実行

2. **利用可能なレビュー観点:**

   - **comments** - コードコメントの正確性と保守性を分析
   - **tests** - テストカバレッジの品質と完全性をレビュー
   - **errors** - サイレントフェイラーのエラーハンドリングをチェック
   - **types** - 型設計と不変条件を分析（新しい型が追加された場合）
   - **code** - プロジェクトガイドラインに対する一般的なコードレビュー
   - **simplify** - 明瞭性と保守性のためにコードを簡素化
   - **all** - 該当するすべてのレビューを実行（デフォルト）

3. **変更されたファイルの特定**
   - `git diff --name-only` で変更されたファイルを確認
   - PR が既に存在するか確認: `gh pr view`
   - ファイルタイプを特定し、どのレビューが適用されるか判断

4. **適用されるレビューの決定**

   変更内容に基づいて:
   - **常に適用**: code-reviewer（一般的な品質）
   - **テストファイルが変更された場合**: pr-test-analyzer
   - **コメント/ドキュメントが追加された場合**: comment-analyzer
   - **エラーハンドリングが変更された場合**: silent-failure-hunter
   - **型が追加/変更された場合**: type-design-analyzer
   - **レビュー通過後**: code-simplifier（最終仕上げ）

5. **レビューエージェントの起動**

   **順次アプローチ**（1つずつ）:
   - 理解しやすく対応しやすい
   - 各レポートが次のレポートの前に完了
   - インタラクティブレビューに適している

   **並列アプローチ**（ユーザーが要求可能）:
   - すべてのエージェントを同時に起動
   - 包括的なレビューでより高速
   - 結果がまとめて返される

6. **結果の集約**

   エージェント完了後にサマリー:
   - **重大な問題**（マージ前に修正必須）
   - **重要な問題**（修正すべき）
   - **提案**（あると良い）
   - **ポジティブな所見**（良い点）

7. **アクションプランの提供**

   発見事項を整理:
   ```markdown
   # PR レビューサマリー

   ## 重大な問題（X 件発見）
   - [エージェント名]: 問題の説明 [ファイル:行]

   ## 重要な問題（X 件発見）
   - [エージェント名]: 問題の説明 [ファイル:行]

   ## 提案（X 件発見）
   - [エージェント名]: 提案 [ファイル:行]

   ## 良い点
   - この PR で良くできている点

   ## 推奨アクション
   1. まず重大な問題を修正
   2. 重要な問題に対応
   3. 提案を検討
   4. 修正後にレビューを再実行
   ```

## 使用例:

**フルレビュー（デフォルト）:**
```
/pr-review-toolkit:review-pr
```

**特定の観点:**
```
/pr-review-toolkit:review-pr tests errors
# テストカバレッジとエラーハンドリングのみレビュー

/pr-review-toolkit:review-pr comments
# コードコメントのみレビュー

/pr-review-toolkit:review-pr simplify
# レビュー通過後にコードを簡素化
```

**並列レビュー:**
```
/pr-review-toolkit:review-pr all parallel
# すべてのエージェントを並列で起動
```

## エージェントの説明:

**comment-analyzer**:
- コメントとコードの正確性を検証
- コメントの腐敗を特定
- ドキュメントの完全性をチェック

**pr-test-analyzer**:
- 振る舞いテストのカバレッジをレビュー
- 重大なギャップを特定
- テスト品質を評価

**silent-failure-hunter**:
- サイレントフェイラーを発見
- catch ブロックをレビュー
- エラーログをチェック

**type-design-analyzer**:
- 型のカプセル化を分析
- 不変条件の表現をレビュー
- 型設計の品質を評価

**code-reviewer**:
- CLAUDE.md への準拠をチェック
- バグと問題を検出
- 一般的なコード品質をレビュー

**code-simplifier**:
- 複雑なコードを簡素化
- 明瞭性と可読性を向上
- プロジェクト標準を適用
- 機能を保持

## ヒント:

- **早めに実行**: PR 作成後ではなく、作成前に
- **変更に焦点**: エージェントはデフォルトで git diff を分析
- **重大な問題から対処**: 優先度の低い問題より先に高優先度の問題を修正
- **修正後に再実行**: 問題が解決されたことを確認
- **特定のレビューを使用**: 懸念点がわかっている場合は特定の観点をターゲット

## ワークフローへの統合:

**コミット前:**
```
1. コードを書く
2. 実行: /pr-review-toolkit:review-pr code errors
3. 重大な問題を修正
4. コミット
```

**PR 作成前:**
```
1. すべての変更をステージング
2. 実行: /pr-review-toolkit:review-pr all
3. すべての重大・重要な問題に対応
4. 特定のレビューを再実行して確認
5. PR を作成
```

**PR フィードバック後:**
```
1. 要求された変更を実施
2. フィードバックに基づいたターゲットレビューを実行
3. 問題が解決されたことを確認
4. 更新をプッシュ
```

## 備考:

- エージェントは自律的に実行され、詳細なレポートを返す
- 各エージェントは深い分析のために専門分野に特化
- 結果は具体的なファイル:行の参照を含むアクション可能な情報
- エージェントはその複雑さに適したモデルを使用
- すべてのエージェントは `/agents` リストで利用可能
