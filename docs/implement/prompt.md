<!-- create-planやimplement-planを実行したときにslackに通知するとして、重要な通知のみを送信するようにしたい
現状のコードだと毎編集ごとに通知が飛んできてlsコマンドすら飛んできてしまう
おそらくサブエージェントの終了時にも完了通知が来ている

co -->

<!-- git commit pushを禁止
gemini禁止

pkillコマンドも禁止（確認）
installコマンドも禁止（確認）
URL取得も禁止（確認）
curl, wgetも禁止（確認） -->
<!-- この問題が発生してないか確認して -->




<!-- proposal.mdを見ても
どんなとき
どのファイルが
どんな処理
を行うのかすぐにわからないのでこのファイルに簡単に書いて -->

<!-- daemonに関しては該当のプロジェクトでdockerコンテナとしての動作実績あり -->


<!-- `slack_approval.py`　     Slackで承認を求め、承認されるまでブロック
`notify-slack.py`　       Slackにボタン付きメッセージを送信し、遠隔で承認・拒否
`edit-approval.py`　      Slackで編集承認を求める（plan.md がある場合はスキップ）
`stop-notify.py`　        Slackに完了通知を送信
`slack_socket_daemon.py`　Socket Modeでボタン応答をIPCで各スクリプトに中継 -->

<!-- ### どんなとき → どのファイルが → どんな処理
| 危険なBashコマンド実行時（rm, git push --force 等） |  |  |
| 計画承認・質問への回答が必要な時（ExitPlanMode / AskUserQuestion） |  | Slackにボタン付きメッセージを送信し、遠隔で承認・拒否 |
| plan.md なしでコードファイルを編集しようとした時 | `edit-approval.py` | Slackで編集承認を求める（plan.md がある場合はスキップ） |
| タスク完了時 | `stop-notify.py` | Slackに完了通知を送信 |
| ボタンクリックでの承認を受け付ける常駐プロセス | `slack_socket_daemon.py` | Socket Modeでボタン応答をIPCで各スクリプトに中継 |

> すべてのスクリプトは `.claude/hooks/` に格納。`settings.json` の hooks セクションで登録して発動する。 -->

<!-- slack通知に関して全体再構築
理由
・不完全な動作や意図してない動作が頻繁に発生するため

作成意図
・席を離れていてもclaudeに指示（承認・拒否）できるようにするため
・進捗確認を遠隔で行うため

ほしい機能
・slackのボタンで承認、拒否を行える
・完了通知

承認が必要なとき
-> 計画書なしで直接編集を行うとき
-> ファイル or フォルダの削除 -->

<!-- skills以外にも重複・競合・下位互換のものがないか調べて削除か既存のものに統合して -->

<!-- .claudeを100点満点で何点か評価し、減点した部分や足りない部分を教えて -->

<!-- _shared/フォルダは削除予定で.claudeに統合予定
.claudeにはなくて_sharedにあるものを移植してきて、ただし下位互換のものや統合済みのものは削除して
 -->

<!-- githubのリポジトリからコピーしてきて使ってないものや英語で書かれているファイルがあってこのプロジェクトを100%使いこなせていない
全ファイル、フォルダの解説（使用するタイミングとどんな効果があるか）をまとめたファイルを.claudeに作成するか.claude\STRUCTURE.mdを上書き -->
