---
tags: ['session', 'automation']
scope: session
date: 2026-03-10
---

# スキル候補レポート — 2026-03-10 09:39

## 検出されたパターン

- **Codex CLI がハングする問題が2回発生** — `codex exec` をバックグラウンド実行後、review.md が更新されないまま停止。`TaskStop` で強制終了後、サブエージェントでレビューを代替実行
- **`/create-proposal` 実行中に要件の曖昧さで対話が発生** — remote-control 時の挙動について「土日も無効」「時間外は端末 UI で承認できる」など、proposal 生成後に仕様が段階的に確定した
- **`is_work_hours()` を4つの独立スクリプトに個別注入** — 共通ユーティリティを作成し全フックに手動で挿入する作業が繰り返された
- **Slack 承認フックが Codex の実行を妨害** — `codex exec` コマンドに Slack 承認フックが反応してプロセスが停止するパターンが再発

---

## スキル候補

| スキル名 | トリガー条件 | 内容 | 優先度 |
|---------|------------|------|--------|
| `codex-review-fallback` | Codex レビューが2回以上タイムアウト・ハングした場合 | サブエージェントを使ったフォールバックレビュー処理を自動起動。`codex-review` スキル内の `TaskOutput` タイムアウト時に自動切り替えるロジックを追加 | 高 |
| `hook-broadcast` | 「全フックに〇〇を追加」「全 Slack フックに時間チェックを入れて」などと言われたとき | `.claude/hooks/slack/*.py` に対し共通の変更パターン（冒頭に `is_xxx()` チェックを挿入するなど）を一括適用するスキル | 中 |

---

## フック候補

| フック種別 | トリガー | アクション | 優先度 |
|-----------|---------|-----------|--------|
| `PreToolUse:Bash` | `codex exec` コマンドが検出されたとき | Slack 承認フックの対象から `codex` コマンドを除外する allowlist エントリを追加。`slack_approval.py` の `classification == "skip"` 条件に `codex exec` を追加 | 高 |

---

## 観察事項

- **要件の段階的な確定パターン** — `/create-proposal` 実行後に「土日も無効」「端末 UI で承認できる」と要件が追記された。`brainstorming` スキルを `/create-proposal` 実行前に活用することで、曖昧さを事前に解消できる可能性がある（ただし自動化というより運用の提案）
- **plan.md の既存内容が上書き対象** — 同一ディレクトリに古い設計書（Phase 2）が存在し、`Write` ツールが「先に Read して」エラーを返す場面があった。`create-plan` スキルの冒頭で既存 plan.md の警告チェックを行うと安全性が上がる