# 編集パターン分析 — 2026-03-03 16:52

## 1. 繰り返し編集パターン

### `docs/implement/plan.md` への大量手戻り（20回以上）

セッション `db8eb919` で `docs/implement/plan.md` が**20回以上**編集された。編集内容を追うと、`/debug` ルートのセキュリティ設計が以下のように3段階で揺れ戻しを繰り返している：

| 段階 | session_edit_count | 設計方針 |
|------|-------------------|---------|
| 初期 | 4 | `department_id` をリクエストパラメータとして受け取る |
| 修正1 | 33 | `ProductSearchRequest` に `in:0,1,2,3,4,5,6,7` バリデーション追加 |
| 修正2 | 39〜41 | `auth()->user()->department_id` をサーバー側で取得（最終案）|
| 後追い修正 | 44〜56 | 修正2に合わせて他の箇所も一斉修正 |

**根本原因推定**: 設計方針が定まる前に `plan.md` への記載が始まり、設計変更のたびに全文を後追い修正する羽目になっている。

### `.claude/hooks/pre-compact-handover.py` への断続的編集（3回）

session_edit_count 2, 3, 5 で同じファイルを編集。import 追加 → EDIT_ANALYSIS_PROMPT 定義 → edit history チェック追加 と、**機能を1ステップずつ追加**するパターン。まとめて1回で書ける内容だった可能性がある。

### `tests/unit/test_masking.py` の修正（edit_count 47→48）

47 でテスト定数を書いた直後に 48 で修正している。テストの期待値が実装の仕様（マスキング後の文字列）を確認してから書かれなかったためと推定。

---

## 2. 問題のある編集パターン

### 構文エラーを出した編集

| ファイル | edit_count | old_string | new_string | syntax_ok |
|---------|------------|------------|------------|-----------|
| `/tmp/test_syntax_err.py` | 3 (session b51de0b8) | `def foo(` | `def foo(:` | `false` |
| `bad.py` | 44 (session db8eb919) | `""` | `def foo(\n` | `false` |
| `bad.py` | 51 (session db8eb919) | `""` | `def foo(\n` | `false` |

`bad.py` の同一構文エラー編集が **edit_count 44と51で完全に同一** — これはedit-trackerのテスト用フィクスチャとして意図的に繰り返されたもの（後述）。

### テスト用フィクスチャ編集の重複（意図的）

以下3セットが session_edit_count 43-45 と 50-52 で**完全に同一**繰り返されている：

```
test.py:   x = 1 → x = 2       (syntax_ok: true)
bad.py:    "" → def foo(\n      (syntax_ok: false)
config.py: API_KEY="" → MASKED  (syntax_ok: true)
```

edit-tracker フックの動作テストのために意図的に生成されたテストケースと判断。ただし、テストスクリプト内にハードコードするより `pytest` フィクスチャとして管理すべきパターン。

---

## 3. Hook 候補

| 検出パターン | 防止方法 | Hook 種別 | 実装案 |
|-------------|---------|-----------|--------|
| `syntax_ok: false` の編集 | 保存前に警告・中断 | PreToolUse (Edit/Write) | `ast.parse()` で事前検証し、失敗時はユーザーに確認 |
| `docs/implement/plan.md` が10回以上編集された場合 | 「全文書き直しを推奨」と通知 | PostToolUse (Edit) | 同一ファイルの edit_count が閾値超えで警告 |
| `affected_files` に常に同じファイル群が含まれる場合 | 連動ファイルの存在を通知 | PostToolUse (Edit) | affected_files の出現頻度を集計し、高頻度ペアを提示 |

---

## 4. Skill 候補

| 繰り返しパターン | 自動化方法 | Skill 名 | トリガー条件 |
|----------------|-----------|----------|-------------|
| `tests/__init__.py` → `tests/unit/__init__.py` → `tests/integration/__init__.py` → `conftest.py` を順番に Write | 一括スキャフォールド | `test-scaffold` | 「テストディレクトリを作って」「pytest の初期設定」 |
| 3コマンドファイル（`learn-edits.md`, `materialize.md`, `review-staged.md`）の Write | コマンドセットを一括生成 | `create-command` | 「新しいコマンドを作って」 |

---

## 5. 学習事項

| 観察 | 原因推定 | 改善アクション |
|------|---------|---------------|
| `plan.md` が20回以上手戻り編集された | 設計決定（`department_id` の取得元）が途中で変わったのに、先行して記載していた箇所を逐次修正した | 設計の核心（認可・データ取得元）を先に固めてから `plan.md` に書き始める。`plan.md` はドラフト→レビュー→確定の1サイクルで完成させる |
| `pre-compact-handover.py` を3回に分けて機能追加した | 機能の全体像を把握してから編集を始めていない | ファイルを Read してから編集計画を立て、1回の Edit で完結させる |
| `test_masking.py` の期待値を書いた直後に修正した | 実装の出力仕様（マスキング後文字列）を実装コードで確認してからテストを書かなかった | テストを書く前に `mask_sensitive()` の戻り値を `REPL` または既存テストで確認してから記述する |