# スキル候補レポート — 2026-03-06 08:25

## 検出されたパターン

- フックスクリプトが `ModuleNotFoundError` でサイレントに失敗していた（症状: 出力なし）
- `sys.path.insert` の参照先ディレクトリが誤っていた（`session/` → `hooks/` が正しい）
- フックのデバッグフローが毎回同じ手順: 「ファイル確認 → スクリプト読み → sys.path 確認 → 手動実行で再現」

---

## スキル候補

| スキル名 | トリガー条件 | 内容 | 優先度 |
|---------|------------|------|--------|
| `hook-debug` | 「hookが動いていない」「hookが反応しない」「出力されない」 | 指定フックを手動で `echo '{}' \| python3 <hook>` 実行し、エラーを即座に表示。`sys.path`・import・settings.json の登録状況を自動チェックする | 高 |

---

## フック候補

| フック種別 | トリガー | アクション | 優先度 |
|-----------|---------|-----------|--------|
| PostToolUse | `Write` / `Edit` でフックファイル（`.claude/hooks/**/*.py`）を変更したとき | 保存直後に `echo '{}' \| python3 <該当ファイル>` を実行して import エラーを即時検出 | 高 |

---

## 観察事項

- `AskUserQuestion` フックの承認フローが割り込んで会話が中断した。Slack 承認が不要なケースの skip 条件（`approval_skip_patterns.txt`）を見直す余地がある
- フックスクリプトの `sys.path` 設定ミスは再発しやすいパターン。`hooks/lib/` 配下の共通モジュールを参照するスクリプトは、パス設定のテンプレートをドキュメント化しておくと防げる