---
tags: ['session', 'edit-patterns']
scope: session
date: 2026-03-11
---

# 編集パターン分析 — 2026-03-11 22:43

---

## 1. 繰り返し編集パターン

### 1-1. `docs/implement/proposal.md` の頻繁な上書き（最多：同一セッションで8回）

セッション `a15a2663` だけで proposal.md を6回 Write（全上書き）。セッション `4e0e035f` でも複数回。

| 時刻 | 内容 |
|------|------|
| 00:37:38 | 在庫テーブル描画改善 |
| 02:56:53 | セル選択スタイル変更 |
| 04:18:35 | RETRY-001 outline（2回目） |
| 06:23:32 | テキスト選択解除 |
| 07:20:45 | 灰色背景削除 |
| 07:42:19 | 灰色背景コメントアウト |

**問題**: 計画が安定しないまま実装に入り、「研究と実装分離」原則が守られていない。`plan.md` も同様に複数回 Write されており、plan-rewrite-alert フック（staging に存在）の有効化が必要。

---

### 1-2. `src/public/js/excel_range_select_copy.js` への集中的な繰り返し編集

セッション `a15a2663` で13回以上、`f279bd8f` で7回以上、`4e0e035f` でさらに追加。

繰り返された問題:
- **outline / box-shadow 表示**: td に適用 → input に適用 → `::after` 疑似要素に変更（3回試行）
- **テキスト選択解除**: `event.preventDefault()` の位置変更を2回試行
- **Delete/Backspace 一括クリア**: `tasks/fix-attempts.md` が3回目であることを記録

---

### 1-3. `.claude/settings.json` の短時間内での試行錯誤

セッション `0faad0cd` → `a15a2663` の間で `disabledMcpjsonServers` が3回変更：

```
["serena"] → ["playwright"] → ["playwright", "serena"]
```

さらに `PreToolUse` matcher が `"Bash"` → `"ExitPlanMode|AskUserQuestion"` → `"Bash"` と revert されている（14回目 → 15回目）。

**原因**: 変更の効果を確認せずに連続変更していた。

---

### 1-4. `docker-compose.yml` の phpmyadmin 設定 revert

| 時刻 | 変更内容 |
|------|----------|
| 23:25:19 | `PMA_HOSTS: mariadb,purchase_mariadb` に変更 |
| 00:12:17 | `PMA_HOST: mariadb` に revert |

**原因**: `PMA_HOSTS` が実際のイメージでサポートされていなかった。

---

## 2. 影響分析パターン

### 2-1. `proposal.md` Write → `plan.md` Write のセット

ほぼ毎回セットで変更されており、create-proposal → create-plan のパイプラインが定着している。

### 2-2. `excel_range_select_copy.js` 編集 → `stock.css` 編集

JS 側で新クラス（例: `COL_SELECTED_CLASS`, `excel-border-cell`）を追加した後、CSS 側の対応定義を追加する流れが繰り返されている。`js-css-sync-check` スキルが有効なユースケース。

### 2-3. Blade インライン JS 削除 → 外部 JS ファイル新規作成のセット

セッション `4e0e035f` で以下のペアが大量に発生：

| Blade ファイル | 抽出先 JS ファイル |
|----------------|-------------------|
| `inbound.blade.php` | `excel_paste_init.js` |
| `stock.blade.php` | `excel_init.js` |
| `order.blade.php` | `excel_paste_init.js` |
| `outbound.blade.php` | `excel_paste_init.js` |
| `login.blade.php` | `login.js` |
| `daily_items.blade.php` | `analytics_year_picker.js` |
| `confirm.blade.php` | `manager_confirm_init.js` |

**規則性**: インライン JS の外部化が一斉に実施された。`no-inline-js.md` ルールが作成されたことがトリガー。

---

## 3. 問題のある編集パターン

### 3-1. 同じバグへの3回試行（`excel_range_select_copy.js` outline 表示）

`tasks/fix-attempts.md` に記録済み：
- **試行1**: `td` に `inset box-shadow` → input 要素が覆って不可視
- **試行2**: `input` に `box-shadow` → 期待どおりにならず
- **試行3**: `td::after` 疑似要素オーバーレイ → 成功

2回失敗時点で `fix-escalation` スキルを呼ぶべきだった。

### 3-2. Delete/Backspace 処理の条件分岐が3回書き直された

```
セッション f279bd8f: 条件追加 → 即日 revert（40分後）
セッション 4e0e035f: 再度追加
```

`RETRY-001` として plan.md に記録されており、根本原因の調査（`document.activeElement` の挙動確認）が不十分なまま実装を繰り返した。

### 3-3. `order.blade.php` の daily_items パネル配置が4回変更

```
components_title 外のフロート → order_table_wrapper 内 → component_area 外 → home.blade.php の switch_content 外（最終）
```

レイアウト設計の事前確認なしに実装を繰り返した。

---

## Hook 候補

| 検出パターン | 防止方法 | Hook 種別 | 実装案 |
|-------------|---------|-----------|--------|
| `proposal.md` / `plan.md` の Write が3回以上 | 警告して計画安定後に実装するよう促す | PostToolUse(Write) | `.claude/staging/plan-rewrite-alert/plan-rewrite-alert.py`（staging に存在、有効化未済） |
| Edit で20行以上削除 | アプローチピボットを検知して警告 | PostToolUse(Edit) | `.claude/staging/large-delete-guard/large-delete-guard.py`（staging に存在、有効化未済） |

---

## Skill 候補

| 繰り返しパターン | 自動化方法 | Skill 名 | トリガー条件 |
|----------------|-----------|----------|-------------|
| JS UI変更後の CSS 同期確認漏れ | JS 変更後に対応 CSS ファイルのクラス名差分をチェック | `js-css-sync-check` | 既にスキルとして実装済み、さらなる活用を推奨 |
| 同じバグへの2回目失敗 | 仮説・試行・結果の記録と調査強制 | `fix-escalation` | `RETRY-` prefix の要件が登場したとき（既存スキル） |
| Blade インライン JS の外部化 | 対象 Blade をスキャンしてインライン JS を検出・外部化 | `extract-inline-js` | 未実装、今回の一斉外部化が自動化候補 |

---

## 学習事項

| 観察 | 原因推定 | 改善アクション |
|------|---------|---------------|
| `proposal.md` が同一セッションで6〜8回上書きされた | 要件が実装中に変化し、計画が常に変わった | `plan-rewrite-alert` フックを staging から hooks/ に昇格させて有効化する |
| `excel_range_select_copy.js` の outline 問題が3回試行 | `input` が `td` を覆う DOM 構造を事前に確認せずに実装 | 修正前に「DOM ツリーを確認する」手順を `verify-before-fix` スキルに追加する |
| `settings.json` が短時間で3回変更 | MCP 無効化の正しいキー名が不明確だった | MEMORY.md に「`disabledMcpjsonServers` が正しいキー名」として記録済み（次セッションで参照できる） |
| `daily_items` パネルのレイアウトが4回変更 | レイアウト変更は実際の画面を見ながら進めるため、Blade のネスト構造の確認が後手 | レイアウト変更前に親要素の構造を Grep + Read で確認するステップを癖づける |
| Codex レビューが12回繰り返された（最大） | plan.md の記述精度が初回から低く、毎回 CHANGES_REQUIRED を受けた | `proposal-quality-gate` スキルを plan.md 作成前に自動適用して品質を上げる |