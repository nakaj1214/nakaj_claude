---
tags: ['session', 'edit-patterns']
scope: session
date: 2026-03-06
---

## 編集パターン分析 — 2026-03-06 12:03

---

## 1. 繰り返し編集パターン

### `.claude/rules/evidence-based-fix.md` への連続編集（6回）

`ts: 02:26:38` から `02:35:28` にかけて、同一ファイルへ6回の個別 Edit が発生。いずれも `Playwright` → `antigravity` への置換。

```
edit 3:  "Playwright CLI / antigravity" → "antigravity IDE"
edit 4:  テーブル行の置換
edit 5:  「Playwright / antigravity / ログ / テスト」→「antigravity / ログ / テスト」
edit 6:  チェックリスト行
edit 12: 別箇所の同パターン（7分後）
```

同様のパターンが `.claude/skills/verify-before-fix/INSTRUCTIONS.md` に対しても7回（edit 13〜19）繰り返された。

**原因推定**: 一括置換ではなく箇所ごとに個別 Edit を実行したため手数が増加。

---

### `browser-verification.md` の二重 Write（edit 2 と edit 21）

`ts: 02:26:32` と `02:36:52` に同一ファイル `.claude/skills/verify-before-fix/references/browser-verification.md` を2度 Write している（`old_string` が両方とも空＝新規作成）。

**原因推定**: 最初の Write（session_id の edit 2）はセッション引き継ぎ用の `HANDOVER-*.md` ではなく別パスに誤って作成した可能性。または2セッションで同じファイルを再生成した。

---

### `table_ui.js` と `datatable.js` のセット編集（複数回）

| 回 | 時刻 | 内容 |
|----|------|------|
| 1回目 | 02:27:58〜02:28:00 | セレクタを `$(datatable.table().container()).find()` 形式に統一 |
| 2回目 | 02:50:33 | デバッグログを V2 に更新 |
| 3回目 | 02:55:22〜02:55:38 | `bind_scroll()` メソッドを切り出し、`initComplete` から呼び出す構造に変更 |

`table_ui.js` 修正 → `datatable.js` 修正のペアが繰り返し発生。設計変更に伴う追随修正のパターン。

---

## 2. 影響分析パターン

### `table_ui.js` → `datatable.js` の連動

`table_ui.js` の API 変更（`bind_scroll()` メソッド追加）を受けて、必ず `datatable.js` 側で呼び出しコードの追加が必要になる構造。  
毎回セットで修正される **密結合ペア**。

### `evidence-based-fix.md` / `INSTRUCTIONS.md` / `SKILL.md` の三連動

`Playwright` 削除方針の適用先が3ファイルに散在しており、1つの意思決定が3ファイルへの変更を引き起こす構造。

---

## 3. 問題のある編集パターン

### デバッグログの残存と後始末（edit 25〜29）

`02:58:18` 〜 `02:58:39` に `[DEBUG]` `console.log` を5行まとめて削除。これは `02:50:33` のデバッグログ追加（edit 22）の後始末。  
実装中にデバッグログを大量追加し、動作確認後に削除するサイクルが発生している。

---

## Hook 候補

| 検出パターン | 防止方法 | Hook 種別 | 実装案 |
|-------------|---------|-----------|--------|
| `[DEBUG]` を含む `console.log` のコミット | コミット前に `[DEBUG]` ログを検出して警告 | `PostToolUse(Bash)` on `git commit` | `grep -rn '\[DEBUG\]' src/public/js/` でヒットしたら警告メッセージ表示 |

---

## Skill 候補

| 繰り返しパターン | 自動化方法 | Skill 名 | トリガー条件 |
|----------------|-----------|----------|-------------|
| 複数ファイルにわたる同一テキスト一括置換（`Playwright` → `antigravity` のような全置換） | `sed` or `Grep` + `Edit` を一括実行するスクリプト化 | `bulk-text-replace` | 「〜を〜に全置換して」「全ファイルで〜を削除して」 |

---

## 学習事項

| 観察 | 原因推定 | 改善アクション |
|------|---------|---------------|
| `evidence-based-fix.md` と `INSTRUCTIONS.md` への「Playwright 削除」が合計13回の個別 Edit に分散した | 同じ文字列パターンが複数箇所に存在し、一括置換ツールを使わず手動で逐次 Edit した | `Grep` で対象箇所を事前に全列挙し、1ステップで `sed` または Bash の `find | xargs sed` で一括置換する |
| `browser-verification.md` が2回 Write された | セッションをまたいで同じファイルを再作成した疑い | Write 前に `Glob` でファイル存在を確認してから書くことを習慣化する |
| `table_ui.js` と `datatable.js` は常にセット修正が必要 | `bind_scroll()` などのインターフェースが両ファイルにまたがる密結合設計 | 修正時は「この変更で `datatable.js` への影響はないか？」を必ずチェックリストに含める |