# Handover — 2026-03-06 10:39

## Session Summary
「推測で修正 → 失敗 → また推測で修正」のループを防止するため、`evidence-based-fix.md` ルールと `verify-before-fix` スキルを新規作成した。さらに antigravity 連携を責務分離してブラウザ検証モジュールを独立させる作業の途中でセッションが終了した。

## Completed Work
- `.claude/rules/evidence-based-fix.md` を新規作成（常時ロード・全言語対応・antigravity 連携記載）
- `.claude/docs/lessons.md` に「推測で修正してはならない」教訓を追記
- `.claude/skills/verify-before-fix/SKILL.md` を作成（トリガー・description 定義）
- `.claude/skills/verify-before-fix/INSTRUCTIONS.md` を作成（5フェーズプロセス定義）
- `.claude/skills/verify-before-fix/evaluations/evals.json` を作成
- `.claude/meta/generate-registry.py` でスキルレジストリを更新（56スキル登録確認）
- Playwright 1.58.2 + Chrome がインストール済みであることを確認
- `.claude/skills/verify-before-fix/scripts/browser-verify.js` を作成（Playwright スクリプト）
- `.claude/skills/verify-before-fix/references/browser-verification.md` を作成（ブラウザ検証参照ドキュメント）

## Incomplete Work
- [ ] `evidence-based-fix.md` の antigravity 固有記述を削除し、Playwright 参照に置き換える — Edit ツールで `| **フロントエンド (JS/CSS/HTML)** | antigravity でスクリーンショット...` の行を編集しようとしたが途中で切断
- [ ] `verify-before-fix/INSTRUCTIONS.md` から antigravity 固有記述を削除し、`references/browser-verification.md` への参照に変更
- [ ] `verify-before-fix/SKILL.md` の description から antigravity への言及を削除または汎用化
- [ ] 実際に連携テストを実施（Playwright スクリプトで実際にブラウザ操作できるか確認）

## Key Decisions
| Decision | Reason |
|----------|--------|
| Playwright CLI を採用（MCP/CLI 両候補から） | `npx playwright` が既インストール済みで即利用可能、Claude Code の Bash ツールから直接呼び出せる |
| antigravity 連携を `references/browser-verification.md` に分離 | ルール・スキル本体を antigravity 依存にしない責務分離のため |
| `verify-before-fix` を `systematic-debugging` と並立させる | verify-before-fix は「修正着手前のゲート」、systematic-debugging は「プロセス全体」として役割が異なる |

## Issues & Solutions
- `python` コマンドが見つからずレジストリ更新スクリプトが失敗 → `python3` で実行して解決

## Gotchas & Notes
- `antigravity/skills/` ディレクトリは存在するが、Claude Code との直接連携方法は未確認。Playwright CLI 経由が現実的
- `verify-before-fix` スキルは description で「バグ修正・改善されていない・直して」というトリガーワードを設定済み。proposal.md に修正依頼がある場合も対象
- Playwright は `npx playwright` で呼び出す（グローバルインストールなし）

## Next Steps
1. `evidence-based-fix.md` を編集して antigravity 記述を削除し、「フロントエンドは `browser-verify.js` スクリプトを使う」に変更
2. `verify-before-fix/INSTRUCTIONS.md` も同様に antigravity 記述を `references/browser-verification.md` 参照に変更
3. `npx playwright screenshot http://localhost <file>` 等で実際にブラウザ操作が通るか動作確認
4. 動作確認後 evals.json でスキルをテストして挙動を検証

## Important Files
| File | Role | Status |
|------|------|--------|
| `.claude/rules/evidence-based-fix.md` | 常時ロードされる「証拠に基づく修正」ルール | changed（要: antigravity 記述の削除） |
| `.claude/skills/verify-before-fix/SKILL.md` | スキルのトリガー・description 定義 | new |
| `.claude/skills/verify-before-fix/INSTRUCTIONS.md` | 5フェーズプロセスの詳細手順 | new（要: antigravity 記述の削除） |
| `.claude/skills/verify-before-fix/scripts/browser-verify.js` | Playwright を使ったブラウザ検証スクリプト | new（未テスト） |
| `.claude/skills/verify-before-fix/references/browser-verification.md` | ブラウザ検証の参照ドキュメント | new |
| `.claude/docs/lessons.md` | 教訓の蓄積ファイル | changed |